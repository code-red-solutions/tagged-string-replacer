<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <AssemblyName>CodeRed.TaggedStringReplacer</AssemblyName>
    <RootNamespace>CodeRed.TaggedStringReplacer</RootNamespace>
    <PackageId>CodeRed.TaggedStringReplacer</PackageId>
    <Version>2.0.0</Version>
    <Authors>Paul Sorauer</Authors>
    <Company>Code Red Solutions Pty Ltd</Company>
    <Description>v2 is a breaking change - namespace has shifted - ITaggedStringReplacer has a better method name and internally the string replacer checks to see if a tag exists in the string before attempting to replace it.</Description>
    <PackageProjectUrl>https://github.com/code-red-solutions/tagged-string-replacer</PackageProjectUrl>
    <RepositoryUrl>https://github.com/code-red-solutions/tagged-string-replacer</RepositoryUrl>
    <PackageTags>string;replace text</PackageTags>
  </PropertyGroup>

</Project>
